
<div class="container">
  <div class="row m-4 card-product">

    <div class="row col-4">
      <%= cl_image_tag @duck.photo.key, class: "card-img-top", alt: @duck.title %>
    </div class="col-4">

    <div class="row col-6 card-duck-infos">
      <div>
        <h2> <%= @duck.title %></h2>
        <p><%= @duck.description %></p>
      </div>
        <h2 class="card-duck-pricing">Prix:<%= @duck.price %>€</h2>
      <button class="btn btn-info" type="button" >
        <%= link_to "Je veux le louer", new_duck_booking_path(@duck)%>
      </button>
    </div>

  </div>
</div>
