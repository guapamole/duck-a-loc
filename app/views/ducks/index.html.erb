<div>
  <h1>Bienvenu sur <em>Duck-a-Loc!</em></h1>
  <h2>Le site numéro un de location de canard en plastique</h2>
</div>

<div>
  <p>Veuillez selectionner un canard à louer</p>
  <% @ducks.each do |duck| %>
    <%= link_to duck_path(@ducks) do %>
      <div class="card">
        <%# (<%= cl_image_tag duck.image.key, width: 300, height: 200, crop: :fill, alt: duck.title %>
        <h2><%= duck.title %></h2>
        <p><%= duck.price %></p>
        <p><%= duck.description %></p>
      </div>
    <% end %>
  <% end  %>
</div>
