
<div class="container justify-center ">

  <div class="row border border-light-subtle shadow-sm bg-light-subtle m-5 p-5">
        <h2 class="title">Ajoute un canard à la location  </h2>
    <div class="col-6">

          <div class="d-flex">

              <div class="formconteiner ms-5">
              <p class="card-duck-pricing d-flex align-items-center"></p>

                <%= simple_form_for(@duck)do |f| %>
                  <%= f.input :title %>
                  <%= f.input :price %>
                  <%= f.input :photo, as: :file %>
                  <%= f.button :submit, "Ajouter", class: "btn btn-info"%>
                <% end %>
              </div>
          </div>
    </div>
  </div>
</div>
